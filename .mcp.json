{
  "$comment": "Playwright MCP cho PHIÊN CHÍNH (MAIN tự tay mở trình duyệt). 6 vai dogfood KHÔNG dùng server này — mỗi vai khai inline trong frontmatter của chính nó để có trình duyệt riêng, không giẫm chân nhau. Dùng `npm exec` chứ không `npx`: npx trong PATH có thể là gói standalone đời cũ không hiểu `-y`.",
  "mcpServers": {
    "browser": {
      "command": "npm",
      "args": ["exec", "--yes", "@playwright/mcp@latest", "--", "--isolated"]
    }
  }
}
