# --- Build artifacts (dọn — PROTOCOL §2 luật #9; không commit) ---
__pycache__/
*.py[cod]
.venv/
target/
build/
.gradle/
dist/
out/
.next/
node_modules/
*.class

# --- Secret / env ---
.env
.env.*
!.env.example
deployment/**/.env

# --- Test / phân tích tạm (screenshot/trace dọn — luật #9) ---
playwright-report/
test-results/
*-trace.zip
.viper/

# --- IDE / OS ---
.idea/
.vscode/settings.json
.claude/settings.local.json
.DS_Store
Thumbs.db
*.swp

# --- Bản tham chiếu (đọc để học, KHÔNG track) ---
*.zip
edu-grounding-viper/

# services/ KHÔNG ignore — MAIN code trực tiếp + commit (fork = monorepo-ish).
# Build artifact BÊN TRONG services/ vẫn bị bắt bởi pattern global ở trên.

# reanchor audit log (local)
.reanchor.log
